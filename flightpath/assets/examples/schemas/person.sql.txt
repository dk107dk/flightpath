

CREATE DATABASE IF NOT EXISTS examples;
USE examples;

DROP TABLE IF EXISTS contact;
DROP TABLE IF EXISTS address;
DROP TABLE IF EXISTS person;

CREATE TABLE IF NOT EXISTS person (
        id int,
        given_name varchar(30) not null,
        family_name varchar(30) not null,
        PRIMARY KEY(id)
);

CREATE TABLE IF NOT EXISTS address (
        id int AUTO_INCREMENT,
        person int,
        city varchar(30),
        state varchar(2),
        zipcode varchar(10),
        PRIMARY KEY(id),
        FOREIGN KEY (person) REFERENCES person(id)
);

CREATE TABLE IF NOT EXISTS contact (
        id int AUTO_INCREMENT,
        person int,
        email varchar(100),
        linkedin varchar(255),
        phone varchar(15),
        PRIMARY KEY(id),
        FOREIGN KEY (person) REFERENCES person(id)
);

INSERT INTO person (id, given_name, family_name) VALUES (1, 'jaffie', 'winters');
INSERT INTO address (person, city, state, zipcode) VALUES(1, 'New York', 'NY', '00001');
INSERT INTO contact (person, email, linkedin, phone) VALUES(1, 'nobody@csvpath.org', 'https://www.linkedin.com', '001 212 1212');


